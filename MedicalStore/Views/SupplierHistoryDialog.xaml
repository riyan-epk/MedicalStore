<Window x:Class="MedicalStore.Views.SupplierHistoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Supplier History" Height="600" Width="800"
        WindowStartupLocation="CenterOwner" Background="{StaticResource BgPrimaryBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" CornerRadius="12" Padding="16" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock x:Name="lblSupplierName" FontSize="20" FontWeight="Bold"/>
                <TextBlock x:Name="lblTotalDue" FontSize="16" Foreground="{StaticResource DangerBrush}" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <TabControl Grid.Row="1">
            <TabItem Header="Invoices">
                <DataGrid x:Name="dgInvoices" Style="{StaticResource ModernDataGrid}" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=dd/MM/yyyy}" Width="Auto"/>
                        <DataGridTextColumn Header="Amount" Binding="{Binding TotalAmount, StringFormat=Rs {0:N2}}" Width="Auto"/>
                        <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="2*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Payments">
                <DataGrid x:Name="dgPayments" Style="{StaticResource ModernDataGrid}" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=dd/MM/yyyy}" Width="Auto"/>
                        <DataGridTextColumn Header="Method" Binding="{Binding PaymentMethod}" Width="Auto"/>
                        <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat=Rs {0:N2}}" Width="Auto"/>
                        <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="2*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>



