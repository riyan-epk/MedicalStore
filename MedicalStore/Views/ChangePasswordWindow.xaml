<Window x:Class="MedicalStore.Views.ChangePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Change Default Password"
        Height="350" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Border CornerRadius="14" Background="White">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="2" BlurRadius="18" Opacity="0.2"/>
        </Border.Effect>
        <StackPanel Margin="30,26,30,26">
            <TextBlock Text="Set a New Password" FontSize="19" FontWeight="Bold"
                       Foreground="{StaticResource TextPrimaryBrush}"/>
            <TextBlock Text="For your security, please change the default admin password before continuing."
                       FontSize="12" TextWrapping="Wrap" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,16"/>

            <TextBlock Text="New Password" FontSize="12" FontWeight="SemiBold"
                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
            <PasswordBox x:Name="txtNew" Style="{StaticResource ModernPasswordBox}" Margin="0,0,0,12"/>

            <TextBlock Text="Confirm Password" FontSize="12" FontWeight="SemiBold"
                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
            <PasswordBox x:Name="txtConfirm" Style="{StaticResource ModernPasswordBox}" Margin="0,0,0,8"/>

            <TextBlock x:Name="lblMsg" Text="" FontSize="12" Foreground="{StaticResource DangerBrush}"
                       TextWrapping="Wrap" Margin="0,0,0,8" Visibility="Collapsed"/>

            <Button Content="Save Password" Style="{StaticResource PrimaryButton}"
                    Height="40" FontSize="14" Click="Save_Click"/>
        </StackPanel>
    </Border>
</Window>
